apiVersion: apps/v1
kind: Deployment
metadata:
  name: kursressourcen_backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kursressourcen_backend
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: kursressourcen_backend
    spec:
      nodeSelector:
        "beta.kubernetes.io/os": linux
      containers:
      - name: kursressourcen_backend
        image: sgse2021containerregistry.azurecr.io/kursressourcen_backend:latest
        ports:
        - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: kursressourcen_backend
spec:
  ports:
  - port: 8000
  selector:
    app: kursressourcen_backend
